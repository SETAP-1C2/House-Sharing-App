{
  "dependencies": {
    "express": "^5.1.0",
    "nodemon": "^3.1.10"
  },
  "type": "module",
  "scripts": {
    "dev": "nodemon svr.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
